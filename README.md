# Recommender System

Node/Express API with Neo4j for the recommender system.

## Quick Start

```bash
./bin/tilt-start.sh
```

## Endpoints

- `GET /health` - Liveness check
- `GET /db-health` - Neo4j connectivity check

